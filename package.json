{
  "name": "utility-laravel-app",
  "version": "1.0.0",
  "description": "Laravel Utility Application",
  "scripts": {
    "build": "php artisan config:cache && php artisan route:cache && php artisan view:cache",
    "start": "php artisan serve"
  },
  "engines": {
    "php": "8.1"
  }
}